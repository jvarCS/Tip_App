<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tipper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Primary Brand Color -->
        <item name="colorPrimary">@color/burg</item>
        <item name="colorPrimaryVariant">@color/brown</item>
        <item name="android:statusBarColor">@color/burg</item>
        <!-- Secondary Brand Color -->
        <item name="colorSecondary">@color/grey</item>

        <!-- Background Color -->
        <item name="android:colorBackground">@color/appBackground</item>

    </style>

    <style name="Theme.Tipper" parent="Base.Theme.Tipper" />
</resources>